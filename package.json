{
  "name": "harmonogram-mow",
  "version": "12.5.5",
  "private": true,
  "description": "PWA do grafiku pracy MOW: Gmail, DOCX, historia godzin i Google Calendar przez kanoniczny Render/IMAP; Apps Script tylko dla funkcji Kalendarza i zgodności.",
  "scripts": {
    "test": "node run-tests.mjs",
    "check": "node --check assets/app.js && node --check service-worker.js && node run-tests.mjs"
  },
  "devDependencies": {}
}
